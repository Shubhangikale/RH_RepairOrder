package com.wcs.app.rh.main.controller;

import java.util.Date;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.ResponseBody;
import org.springframework.web.bind.annotation.RestController;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.wcs.app.rh.main.models.CustomerDetails;
import com.wcs.app.rh.main.models.CustomerServiceDetails;
import com.wcs.app.rh.main.models.CustomerVehicle;
import com.wcs.app.rh.main.models.District;
import com.wcs.app.rh.main.models.States;
import com.wcs.app.rh.main.models.VehicleModel;
import com.wcs.app.rh.main.models.VehicleType;
import com.wcs.app.rh.main.serviceimpl.ServiceImplementation;
import com.wcs.app.rh.main.utilities.PreROInvoiceGenerator;


@RestController
@CrossOrigin("*")
public class AccountController
{

	@Autowired
	private PreROInvoiceGenerator preRoGenerator;
	@Autowired
	private ServiceImplementation serviceImpl;
	@CrossOrigin("*")
	@RequestMapping(value="/addcustdetails",method=RequestMethod.POST)
	public String addCustomer(@RequestBody CustomerDetailsAll customerDetailsAll )
	{
		String preRoNumber=preRoGenerator.autoGeneratedPreRoInvoice();
		
		CustomerDetails customerDetails=new CustomerDetails();
		customerDetails.setFirst_name(customerDetailsAll.getFirst_name());
		
		
	CustomerDetails cd=	serviceImpl.savecust(customerDetails);
	
		CustomerVehicle cv=new CustomerVehicle();
		cv.setCustomerDetails(cd);
//		CustomerVehicle csv1=serviceImpl.saveveh(cv)
				
		CustomerServiceDetails csd=new CustomerServiceDetails();
		
//		CustomerServiceDetails csv2=serviceImpl.saveserv(csv1)
		
		
		return "Saved";
	}
	@GetMapping(value="/getStates", produces = "application/json")
	@ResponseBody
	@CrossOrigin("*")
	public String getAllStates() throws JsonProcessingException
	{
		System.out.println("In Account to get states list");
		List<States>  statelist=serviceImpl.findAllStates();
		ObjectMapper objmap=new ObjectMapper();
		String json=objmap.writeValueAsString(statelist);
		return json;
	}
	@GetMapping(value="/getCities", produces = "application/json")
	@ResponseBody
	@CrossOrigin("*")
	public String getDistrict(@RequestParam int id) throws JsonProcessingException
	{
		System.out.println("In Account with state id for get dist " + id);
		List<District>  dlist=serviceImpl.findAllByState_id(id);
		ObjectMapper objmap= new ObjectMapper();
		String json=objmap.writeValueAsString(dlist);
		return json;
	}
	@GetMapping(value="/getVehicle", produces = "application/json")
	@ResponseBody
	@CrossOrigin("*")
	public String getAllVehicle() throws JsonProcessingException
	{
		System.out.println("In Account to get Vehicle Model");
		List<VehicleModel>  vmodel_list=serviceImpl.findAllVehicleModel();
		ObjectMapper objmap=new ObjectMapper();
		String json=objmap.writeValueAsString(vmodel_list);
		return json;
	}
	@GetMapping(value="/getVehicleType", produces = "application/json")
	@ResponseBody
	@CrossOrigin("*")
	public String getVehicleType(@RequestParam int id) throws JsonProcessingException
	{
		System.out.println("In Vehicle Model Type" + id);	
		List<VehicleType>  v_model_type=serviceImpl.findAllByModel_id(id);
		ObjectMapper objmap= new ObjectMapper();
		String json=objmap.writeValueAsString(v_model_type);
		return json;
	}
}
